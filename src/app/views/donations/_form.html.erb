<%# Project name: MusicalStudentsGroup
 Description: Lets school administrators use a platform to contact donators for instruments,
 Setup a systematic way for students to receive lessons, and for students to form their own
 student music groups.
 Filename: _form.html.erb
 Description: Form field for making new / editing donations. provides style, requires fields 
 to be filled and provides list for instruments
 NOTE: This was originally generated by the scaffold framework, but i edited functionality
 and styling of this file.
 Last modified on: 4-26-2020 %>

<style>
div.actions {
  display: block;
  margin: auto;
  text-align: center;
}

body {
  background-color: #F3B47D;
}
</style>

<body>
<%= form_with(model: donation, local: true) do |form| %>
  <% if donation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(donation.errors.count, "error") %> prohibited this donation from being saved:</h2>

      <ul>
        <% donation.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, :required => 'required' %>
  </div>

  <div class="field">
    <%= form.label :instrument %>
    <%= form.select :instrument, ['Flute', 'Oboe', 'Clarinet', 'Saxophone', 'Bassoon', 'Trumpet', 'Trombone', 'Euphonium', 'Tuba', 'Piano', 'Percussion', 'Viola', 'Violin', 'Cello', 'Bass', 'Guitar'], :required => 'required' %>
  </div>

  <div class="field">
    <%= form.label :condition %>
    <%= form.select :condition, ['Good', 'Fair', 'Poor'], :required => 'required' %>
  </div>

  <div class="field">
    <%= form.label :model %>
    <%= form.text_field :model, :required => 'required' %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location, :required => 'required' %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, :required => 'required' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
</body>