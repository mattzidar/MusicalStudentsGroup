<%# Project name: MusicalStudentsGroup
 Description: Lets school administrators use a platform to contact donators for instruments,
 Setup a systematic way for students to receive lessons, and for students to form their own
 student music groups.
 Filename: index.html.erb
 Description: Home page for listing donations
 NOTE: This was originally generated by the scaffold framework, but i edited functionality
 and styling of this file.
 Last modified on: 4-27-2020 %>

<style>
button {
  display: block;
  margin: auto;
}

body {
  background-color: #F3B47D;
}
</style>

<body>
<h1>Donation Listings</h1>

<%-# table setup that lists donations -%>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Instrument</th>
      <th>Condition</th>
      <th>Model</th>
      <th>Location</th>
      <th>Email</th>
      <th colspan="3">Options</th>
    </tr>
  </thead>

  <tbody>
    <% @donations.each do |donation| %>
      <tr>
        <td><%= donation.name %></td>
        <td><%= donation.instrument %></td>
        <td><%= donation.condition %></td>
        <td><%= donation.model %></td>
        <td><%= donation.location %></td>
        <td><%= donation.email %></td>
        <td><%= link_to 'Request Donation', donation %></td>
        <td><%= link_to 'Edit Donation', edit_donation_path(donation) %></td>
        <td><%= link_to 'Delete Donation', donation, method: :delete, data: { confirm: 'Are you sure you want to delete the donation listing?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<button><%= link_to 'New Donation', new_donation_path %></button>
</body>